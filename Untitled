Don't push to GitHub. Just make sure that, whatever creates the number of images and number of videos we have implemented the limit on every pricing section, is actually being implemented. After that, people can't generate images and videos anymore, right? Check that, and also pull the data from all the subscription emails: what are the top users and how many credits they have left and consumed. Give